server.port: 8081

spring:
  jpa:
    generate-ddl: true
    properties.hibernate.dialect: org.hibernate.dialect.MySQL8Dialect

  application:
     name: timesheets-ms

  datasource:
    url: jdbc:mysql://localhost:3306/movies?useSSL=false&user=root&password=000315